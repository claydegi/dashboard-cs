# Railway configuration - Install Python + Node.js
[phases.setup]
aptPkgs = ["python3", "python3-pip"]

[phases.install]
cmds = ["npm install", "pip3 install -r requirements.txt"]

[start]
cmd = "node server.js"
